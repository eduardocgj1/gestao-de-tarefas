# Dependências
node_modules/

# Variáveis de ambiente
.env

# Banco local (não é mais necessário, mas mantido por segurança)
data.json
data.json.bak

# Sistema
.DS_Store

# Claude Code (ignorar só configs locais, não a pasta de skills)
.claude/settings.local.json
.claude/launch.json
